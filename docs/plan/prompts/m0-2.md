# M0.2 — paste this into a new session

```
Read @docs/plan/milestone-plan.md — Global Constraints, Part 1 §1.3 (all four
seams, with their code), W0 · M0, and Part 5 §C9.
Read @SPEC.md §4.2, §4.3, §8.1a, §8.3, §11.1, §11.2, §11.7, §13, §19.6.
Read @CLAUDE.md and @docs/plan/next-session.md §Session 2.

This is M0.2 — the load-bearing session. Everything in waves 2–5 depends on
these mechanisms being correct. Sequential work on main — no worktrees.

ALREADY LANDED IN M0.1 — read them, do not rebuild them:
  - Seam 2: pkgutil discovery in app/main.py and app/models/__init__.py,
    including the ENV == "production" exclusion for the dev router. The
    milestone plan's own snippet is buggy — `app = FastAPI(...)` shadows the
    `app` package, so `app.routers.__path__` resolves against the FastAPI
    instance and discovery silently finds nothing. Shipped version aliases it
    to `routers_pkg`. Keep that.
  - Seam 3: web/packages/i18n — nine namespaces × three locales, index.ts
    authored once with every namespace including the 24 empty stubs.
  - The D2 token seed in web/packages/ui (tokens.css, theme.ts, self-hosted
    Rubik). The full 4h component-library port is M0.3, not this session.
  - CI (.github/workflows/ci.yml) and ./scripts/ci-local.sh — five jobs, green.
  - Railway staging, live: https://api-staging-1e4d.up.railway.app

TWO PREREQUISITES, both missing — handle them first:
  1. No Postgres anywhere. The Railway project `studio-manager` has api/staff/
     parent/dashboard and no database. Add one to staging with
     `railway add --database postgres`, and stand a local one up with Docker
     (psql is not installed on this machine; docker is).
  2. No Postgres driver in .venv. SQLAlchemy 2.0.52 and Alembic 1.19.1 are
     already there; psycopg is not. Add it and put it in requirements-dev.txt,
     or CI's backend job will pass locally and fail on the runner.

BUILD, in this order:

1. The two remaining seams from §1.3:
   - Alembic baseline. alembic/versions/** stays owned by main; the existing
     .claude/hooks/block-protected.sh already denies edits to it.
   - web/packages/ui/src/slots.ts — the slot registry, exactly as the plan
     writes it. Nothing consumes it until M3; land it anyway so no lane has to.

2. Tenancy (§4.2): TenantSession dependency, TenantMixin applying the default
   studio_id filter, and the .with_all_tenants() escape hatch — legal only in
   platform-admin code and deliberate cross-studio jobs. Every tenant-scoped
   table carries a non-null studio_id with a leading composite index.

3. Encryption (§11.1): AES-256-GCM envelope. Keys in Railway secrets, never in
   the database, versioned so rotation does not mean re-encrypting everything.
   M3 needs this for registration_request.payload_encrypted, not just M4.

4. Audit log (§11.2): append-only. The application DB role gets INSERT and no
   UPDATE or DELETE. Assert that against the actual grants, not against a
   comment saying it is so.

5. The log scrubber, and the test that health-declaration fields never
   serialize into log output (G7 — never log their contents).

6. tests/invariants/ — SPEC §13's five non-negotiables. Numbers 3 and 5 assert
   vacuously true until M6 exists; that is correct and intended. They must
   exist now so no lane can land the first violation unnoticed.

7. scripts/lane-check.sh and scripts/i18n-parity.mjs. The plan gives
   lane-check.sh in full. Every lane in every wave runs it as its one command,
   so it has to be right. Note the repo convention M0.1 established: Python
   scripts live in scripts/, Node scripts in web/scripts/ where their deps
   resolve.

CONVENTIONS THIS REPO ALREADY ENFORCES — match them:
  - pyproject.toml holds the ruff/mypy/pytest config. mypy runs strict.
  - Assert behaviour, not source text, wherever behaviour is observable. Where
    only source can be checked (a discovery loop, a DB grant), say so in the
    docstring.
  - Prove a new gate fails before trusting it. M0.1 found three gates that
    passed while checking nothing.
  - ./scripts/ci-local.sh must be green before every push.

Plan first with superpowers:writing-plans, then work the plan task by task:
failing test, confirm it fails, minimal implementation, green, commit.

Exit gate: ./scripts/lane-check.sh core is green, and
.venv/bin/alembic upgrade head runs clean on a fresh database.
```
