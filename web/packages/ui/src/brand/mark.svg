<!-- PLACEHOLDER. §15 item 7 (studio logo and colours) is outstanding and blocks M1.
     D3 restraint: one deep accent on a warm ground, no decoration. The two bars
     read as ranked belts without using belt colours as brand — those are data
     (belt_rank.color_hex, §5.9) and would collide with rank display.
     Replace this file and re-run scripts/generate-icons.mjs. -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <rect width="512" height="512" fill="#f7f5f1"/>
  <rect x="96" y="150" width="320" height="64" rx="8" fill="#17150f"/>
  <rect x="96" y="298" width="320" height="64" rx="8" fill="#1f6b3f"/>
  <rect x="96.5" y="298.5" width="319" height="63" rx="8" fill="none" stroke="#17150f" stroke-width="1"/>
</svg>
