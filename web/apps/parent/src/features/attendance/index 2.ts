// The parent app's attendance feature: 2c's strip section. The absence pre-report has
// its own feature directory (features/absence) — §10.2 treats them differently.
export { registerAttendanceSections } from './StudentCardAttendanceSection'
